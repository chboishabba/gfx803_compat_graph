diff --git a/projects/rocr-runtime/runtime/hsa-runtime/core/runtime/amd_gpu_agent.cpp b/projects/rocr-runtime/runtime/hsa-runtime/core/runtime/amd_gpu_agent.cpp
--- a/projects/rocr-runtime/runtime/hsa-runtime/core/runtime/amd_gpu_agent.cpp
+++ b/projects/rocr-runtime/runtime/hsa-runtime/core/runtime/amd_gpu_agent.cpp
@@
-  // Only DoorbellType 2 (HSA_CAP_DOORBELL_TYPE_2_0) is supported by the HSA runtime.
+  // DoorbellType 1 is re-enabled by the gfx803 compatibility lane together with
+  // the restored legacy queue semantics. DoorbellType 2 remains the native AQL
+  // path. Other legacy/future types continue through the normal unsupported-GPU
+  // skip path rather than poisoning hsa_init() for the rest of the topology.
@@
   switch (node_props.Capability.ui32.DoorbellType) {
     case 2: // HSA_CAP_DOORBELL_TYPE_2_0 — supported
+    case 1: // HSA_CAP_DOORBELL_TYPE_1_0 — restored for Polaris/gfx803
       break;
     case 0: // HSA_CAP_DOORBELL_TYPE_PRE_1_0 — deprecated (Kaveri, Hawaii, Tonga)
-    case 1: // HSA_CAP_DOORBELL_TYPE_1_0 — deprecated (Fiji, Polaris, Vegam)
     default: {
